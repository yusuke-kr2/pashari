<div class="app-header">
  <span class="app-title">Pashari</span>
  <% if user_signed_in? %>
    <%= button_to t('shared.header.logout'), destroy_user_session_path, method: :delete, class: "btn btn-sm btn-link text-secondary p-0 app-header-logout" %>
  <% end %>
</div>
