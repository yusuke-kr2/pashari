<nav class="navbar navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">Pashari</a>
    <div>
      <% if user_signed_in? %>
        <%= link_to t('shared.header.groups'), groups_path, class: "btn btn-outline-light btn-sm me-2" %>
        <%= button_to t('shared.header.logout'), destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
      <% else %>
        <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-light btn-sm me-2" %>
        <%= link_to "新規登録", new_user_registration_path, class: "btn btn-light btn-sm" %>
      <% end %>
    </div>
  </div>
</nav>
